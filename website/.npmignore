# Exclude build artifacts that will be regenerated
src/preview-components/

# Exclude docs output (generated by users)
docs/

# Exclude node_modules (will be installed by users)
node_modules/

# Exclude test files
**/*.test.tsx
**/*.test.ts
**/__tests__/

# Keep source files (src/) for rebuilding with user components
# Keep package.json for dependency management
# Keep public/ for static assets
